# Django framework
Django==4.2.3

# Django REST Framework for building APIs
djangorestframework==3.14.0

# PostgreSQL driver
psycopg2==2.9.7

# Utility for local CPU & memory usage
psutil==5.9.5

# Docker client library (if you need Docker interactions/logs)
docker==6.1.3

# Gunicorn for production WSGI server (optional, but recommended for deployment)
gunicorn==20.1.0

# python-dotenv to manage environment variables if needed
python-dotenv==1.0.0

# Additional libraries (HTTP requests, ping utilities, etc.)
requests==2.31.0
pythonping==1.1.4
